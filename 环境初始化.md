# 环境设置

本项目基于 r-core 的 rust os 项目。根据官方信息，需要在 qemu 下运行
推荐使用 wsl/linux

## 如何设置环境

首先运行 'init_rust.sh' 安装rust
然后重启终端，让rust工具链起效

运行 'init_rust_riscv.sh' 让rust设置riscv支持
运行 'init_qemu.sh' 下载并编译安装 qemu 7.0.0

然后就可以 git clone 进行学习了！

学习资源如下：
https://rcore-os.cn/rCore-Tutorial-Book-v3/index.html